PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
# Leave unset for local Compose defaults or set explicitly to match POSTGRES_PASSWORD in docker-compose.yml
DB_PASSWORD=
DB_DATABASE=appdb
