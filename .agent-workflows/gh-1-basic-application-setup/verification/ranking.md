# Candidate ranking — gh-1-basic-application-setup

## candidate-a (implemented)

- Satisfies all `must` requirements with automated checks.
- Single consolidated worktree; strategies b/c folded per decomposition note.

## candidate-b / candidate-c

- **Not executed** as separate code paths (documented consolidation in `decomposition.md`).

## Relative scores

1. **candidate-a** — complete, minimal surface area, reproducible with `docker compose` + `pnpm`.
2. **candidate-b / c** — n/a (no branch).

Recommendation: select **candidate-a**.
