# Implementation brief — candidate-b (test-first — superseded into candidate-a)

**Status:** Consolidated — no separate worktree.

Original intent: write failing e2e for DB connectivity first, then minimal implementation. Equivalent steps applied within `candidate-a` ordering.
