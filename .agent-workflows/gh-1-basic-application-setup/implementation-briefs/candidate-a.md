# Implementation brief — candidate-a (minimal)

- Smallest Nest surface: HealthModule + AppModule wiring only.
- Single health unit spec + existing e2e pair (health + openapi).
- No extra validation layers beyond `ValidationPipe` in bootstrap.
