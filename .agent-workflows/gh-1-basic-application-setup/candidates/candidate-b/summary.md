## Summary — candidate-b

Same runtime as candidate-a; extra unit test asserting raw errors propagate when `DataSource.query` rejects.

**Requirements:** REQ-001 … REQ-005.

**Strategy:** test-first emphasis on failure path (unit tier).
