#!/usr/bin/env bash
# after-intake: placeholder — ensure intake artifacts committed locally if desired.
set -euo pipefail
echo "after-intake: intake artifacts should exist under .agent-workflows/<task-id>/ (see docs/state-layout.md)"
exit 0
